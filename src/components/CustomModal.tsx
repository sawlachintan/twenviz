import { FC } from "react";
import { Button } from "@mui/material";

type Props = {};

export const CustomModal: FC<Props> = (props) => {
    return (
        <div>
            <Button>Wow</Button>
        </div>
    );
};
